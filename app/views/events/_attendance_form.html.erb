<% if Attendance.where(user: current_user, event:event).exists? %>
  <p>You are attending!</p>
<% else %>
 <%= simple_form_for [event, attendance], data: {attendances_target: "form", action: "submit->attendances#send"} do |f| %>
    <%= f.submit "I am attending" %>
  <% end %>
<% end %>
